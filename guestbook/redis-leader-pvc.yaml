kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: redis-leader-pvc
  namespace: guestbook
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: tanzugoldsp
  resources:
    requests:
      storage: 2Gi
